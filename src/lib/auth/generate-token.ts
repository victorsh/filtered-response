import fs from 'fs'
import crypto from 'crypto'

const generateKey = () => {
  console.log(__dirname)
  // const newSecretKey = crypto.randomBytes(64).toString('hex')
}

(async () => {
  generateKey()
})()
